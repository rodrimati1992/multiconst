mod tests_mod {
    #[cfg(feature = "derive")]
    mod derive_tests;

    mod field_macro_tests;
    mod misc_tests;
    mod slice_destructuring;
    mod struct_destructuring;
    mod tuple_destructuring;
    mod ui_tests;
}
